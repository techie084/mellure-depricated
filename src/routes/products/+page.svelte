<script lang="ts">
	let { data } = $props();
	const { products } = data;
</script>

<h1>products</h1>

{#each products as product}
	<h3>
		<a href="/product/{product.id}">
			{product.title}
		</a>
	</h3>
	<p>{product.description}</p>
	<p>{product.price}</p>
{/each}
