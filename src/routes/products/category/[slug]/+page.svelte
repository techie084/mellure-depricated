<!-- <script lang="ts">
	import SEO from '$lib/components/shared/seo.svelte';
	import { generateSEO } from '$lib/utils/seo';
	import type { PageData } from './$types';

	export let data: PageData;

	const seo = generateSEO({
		title: `${data.category} Products`,
		description: `Browse our collection of ${data.category} products. Find the best deals on quality items.`,
		url: `https://yoursite.com/products/category/${data.slug}`,
		keywords: [data.category, 'shop', 'buy', 'products'],
		type: 'website'
	});
</script>

<SEO config={seo} />

<h1>{data.category} Products</h1> -->
